extends base

block content 
    if errorMessage 
        span=errorMessage
    form(method="post")
        input(placeholder="Old password",name="oldPassword" required, type="password")
        input(placeholder="New password",name="newPassword" required, type="password")
        input(placeholder="New password Comfirm",name="newPasswordComfirm" required, type="password")
        input(type="submit", value="change password")